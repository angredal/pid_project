spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/rating_db
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  application:
    name: rating-service
